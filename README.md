# Horizon-Autoscaler
Horizon — a predictive Kubernetes autoscaler that forecasts request rate/queue depth to proactively scale pods, with a max(predicted, reactive) safety net so it never scales worse than standard HPA.
